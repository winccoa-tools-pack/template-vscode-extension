{
  "name": "winccoa-vscode-extension",
  "displayName": "WinCC OA Tools (template)",
  "publisher": "winccoa-tools-pack",
  "version": "0.0.1",
  "author": "winccoa-tools-pack",
  "license": "MIT",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "dist/extension.js",
  "scripts": {
    "build": "tsc --build",
    "compile": "tsc --build",
    "prepublishOnly": "npm run build",
    "lint": "eslint \"src/**/*.ts\"",
    "lint:fix": "eslint \"src/**/*.ts\" --fix",
    "lint:md": "markdownlint \"**/*.md\" --ignore node_modules",
    "lint:md:fix": "markdownlint \"**/*.md\" --ignore node_modules --fix",
    "format": "prettier --write \"src/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\"",
    "style-check": "npm run lint && npm run format:check && npm run lint:md",
    "style-fix": "npm run lint:fix && npm run format && npm run lint:md:fix",
    "test": "npm run style-check && npm run build && npm run test:unit",
    "test:unit": "npm run build && node --test --test-force-exit test/unit/*.test.ts",
    "test:integration": "npm run build && node --test --test-force-exit test/integration/*.test.ts",
    "ci:integration": "npm ci && npm run build && npm run test:integration",
    "watch": "tsc --build --watch",
    "package": "vsce package",
    "vscode:prepublish": "npm run compile"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^25.0.3",
    "@types/vscode": "^1.107.0",
    "@typescript-eslint/eslint-plugin": "^8.51.0",
    "@typescript-eslint/parser": "^8.51.0",
    "@vscode/test-electron": "^2.3.9",
    "@vscode/vsce": "^3.7.1",
    "eslint": "^9.39.2",
    "glob": "^13.0.0",
    "markdownlint-cli": "^0.47.0",
    "mocha": "^11.7.5",
    "prettier": "^2.8.8",
    "typescript": "^5.2.0"
  }
}
